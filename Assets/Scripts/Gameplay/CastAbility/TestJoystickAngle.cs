using UnityEngine;

public class TestJoystickAngle : MonoBehaviour
{
    public Joystick joystick;

    // Update is called once per frame
    void Update()
    {
        //Debug.Log($"{joystick.Direction} - {joystick.Direction.Angle()}" );
    }
}